import {actionType} from "../Helper";

const initState = []
export const messageBarReducer = (state=initState,action) => {
    switch (action.type) {

        default:
            return state

    }
}